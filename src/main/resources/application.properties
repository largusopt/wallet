spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/wallet}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1607}
spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:db/changelog/db.changelog.xml
#---
spring.config.activate.on-profile=integration-test
spring.datasource.url=${TEST_CONTAINER.URL}
spring.datasource.username=${TEST_CONTAINER.USERNAME}
spring.datasource.password=${TEST_CONTAINER.PASSWORD}
spring.liquibase.enabled=true
spring.liquibase.drop-first=false
spring.liquibase.change-log=classpath:/db/changelog/db.changelog.xml
spring.liquibase.default-schema=public